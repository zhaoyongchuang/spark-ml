package scala

object reduceby_stu {
//
def main(args: Array[String]): Unit = {
//  spark 中的reducebykey
//  reducebykey是对每个key对应的value进行reduce操作
//  use reucebykey to calculate the word count
//  val words = Array("one","two","two","three","three","three")
//  val wordpairs = words.map(word=>(word,1))
//  val wordcounts = wordpairs.reduceByKey((x,y)=>x+y)//解决错误

//  wordcounts.foreach(wordcount=>println(wordcount._1+" : "+wordcount._2))


}
}
